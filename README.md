# PostScript to C# Interpreter
Interprets PostScript commands through console, performs operations in C#, pushes result to stack.

1. Clone Repo
2. Open in VS Community
3. Run Interpreter (Main in Program.cs)
